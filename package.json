{
  "name": "koishi-plugin-apng-two-frame",
  "version": "1.0.0",
  "description": "将两张图片合成为两帧 APNG 动图",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "npm run build && node --test test/*.test.js"
  },
  "keywords": [
    "koishi",
    "plugin",
    "apng",
    "幻影坦克",
    "表情包"
  ],
  "license": "MIT",
  "contributors": [
    "Alec <469405634@qq.com>"
  ],
  "homepage": "https://github.com/DNAlec/koishi-plugin-apng-two-frame",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DNAlec/koishi-plugin-apng-two-frame.git"
  },
  "peerDependencies": {
    "koishi": "^4.0.0"
  },
  "devDependencies": {
    "koishi": "^4.18.11",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  },
  "koishi": {
    "description": {
      "zh": "将两张图片合成为两帧 APNG 动图：第一张作缩略图，点开后显示第二张",
      "en": "Combine two images into a two-frame APNG: the first frame as thumbnail, the second shows when opened"
    },
    "service": {
      "required": [
        "http"
      ]
    }
  }
}
