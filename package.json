{
  "name": "koishi-plugin-apng-two-frame",
  "version": "0.0.1",
  "description": "APNG 两帧处理插件",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "npm run build && node --test test/*.test.js"
  },
  "keywords": [
    "koishi",
    "plugin",
    "apng"
  ],
  "license": "MIT",
  "peerDependencies": {
    "koishi": "^4.0.0"
  },
  "devDependencies": {
    "koishi": "^4.18.11",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
